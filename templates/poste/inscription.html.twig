{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.cv,{'label':'insérer votre cv'}) }}
    <button type="submit" class="btn btn-lg btn-primary w-100 mt-2">valider</button>
    {{ form_end(form) }}
{% endblock %}